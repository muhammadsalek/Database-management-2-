SELECT name, instructor.dept_name, building FROM instructor, department WHERE instructor.dept_name = department.dept_name;
SELECT name, instructor.dept_name, building FROM instructor JOIN department ON instructor.dept_name = department.dept_name;
SELECT name, instructor.dept_name, building FROM instructor, department WHERE instructor.dept_name = department.dept_name;
# Show name, department and building of instructors with salary greater than 60000 SELECT name, instructor.dept_name, building FROM instructor JOIN department ON instructor.dept_name = department.dept_name;
# Show name, department and building of instructors with salary greater than 60000 SELECT * FROM instructor JOIN department ON instructor.dept_name = department.dept_name;
# Show name, department and building of instructors with salary greater than 60000 SELECT * FROM instructor JOIN department ON instructor.dept_name = department.dept_name;
# Show name, department and building of instructors with salary greater than 60000 SELECT * FROM instructor JOIN department ON instructor.dept_name = department.dept_name WHERE salary > 60000;
# Show name, department and building of instructors with salary greater than 60000 SELECT name, instructor.dept_name, building FROM instructor JOIN department ON instructor.dept_name = department.dept_name WHERE salary > 60000;
# Show name, department and building of instructors with salary greater than 60000 SELECT name, instructor.dept_name, building FROM instructor JOIN department ON instructor.dept_name = department.dept_name WHERE salary > 70000;
# Show name, department and building of instructors with salary greater than 60000 SELECT name, instructor.dept_name, building FROM instructor, department WHERE instructor.dept_name = department.dept_name AND salary > 70000;
# Show name, department and building of instructors with salary greater than 70000 SELECT name, instructor.dept_name, building FROM instructor JOIN department ON instructor.dept_name = department.dept_name WHERE salary > 70000;
# Show name, department and building of instructors with salary greater than 70000 SELECT name, instructor.dept_name, building FROM instructor NATURAL JOIN department WHERE salary > 70000;
# Show name, department and building of instructors with salary greater than 70000 SELECT * FROM instructor NATURAL JOIN department WHERE salary > 70000;
SELECT * FROM instructor NATURAL JOIN teaches;
SELECT * FROM instructor JOIN teaches ON instructor.ID = teaches.ID;
SELECT * FROM instructor NATURAL JOIN teaches;
SELECT * FROM student NATURAL JOIN takes;
SELECT * FROM student NATURAL INNER JOIN takes;
SELECT * FROM student NATURAL JOIN takes;
SELECT * FROM student NATURAL LEFT JOIN takes;
SELECT * FROM student NATURAL RIGHT JOIN takes;
SELECT * FROM takes NATURAL RIGHT JOIN course;
SELECT * FROM takes RIGHT JOIN course ON takes.course_id = course.course_id;
SELECT * FROM instructor NATURAL JOIN advisor;
SELECT * FROM instructor JOIN advisor ON instructor.ID = advisor.i_ID;
SELECT * FROM instructor JOIN advisor ON instructor.ID = advisor.i_ID JOIN student ON student.ID = advisor.s_ID;
SELECT instructor.name, student.name FROM instructor JOIN advisor ON instructor.ID = advisor.i_ID JOIN student ON student.ID = advisor.s_ID;
SELECT instructor.name, student.name FROM (instructor LEFT JOIN advisor ON instructor.ID = advisor.i_ID) JOIN student ON student.ID = advisor.s_ID;
SELECT instructor.name, student.name FROM (instructor LEFT JOIN advisor ON instructor.ID = advisor.i_ID) LEFT JOIN student ON student.ID = advisor.s_ID;
SELECT instructor.name AS teacher, student.name AS student FROM (instructor LEFT JOIN advisor ON instructor.ID = advisor.i_ID) LEFT JOIN student ON student.ID = advisor.s_ID;
SELECT * FROM instructor JOIN department ON instructor.dept_name = department.dept_name;
SELECT * FROM instructor, student;
SELECT * FROM instructor NATURAL JOIN student;
SELECT * FROM instructor JOIN student USING (dept_name);
SELECT instructor.name, student.name FROM instructor JOIN student USING (dept_name);
SELECT instructor.name, student.name FROM instructor LEFT JOIN student USING (dept_name);

